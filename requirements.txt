# stock-analyst dependencies
# Python 3.10+ required

# Core data
akshare>=1.18
pandas>=2.0
requests>=2.31  # itick HTTP 客户端（付费源 P2 兜底）；信息储备层 EDGAR 也用它
PySocks>=1.7  # 信息储备层访问 SEC EDGAR 走 SOCKS5 系统代理时需要（requests[socks]）

# Markets
yfinance>=0.2  # 美股数据；需要代理才能稳定访问 Yahoo Finance
# itick (https://api0.itick.org) — 美股 K 线 / info P2 兜底，仅需 requests，无独立 SDK
# 设置 STOCK_ANALYST_ITICK_TOKEN 环境变量启用

# LLM 情感分析 + PDF 综合解读
anthropic>=0.40
httpx>=0.27

# PDF 渲染
markdown>=3.5
matplotlib>=3.7  # 多因子打分卡的雷达图

# 因子计算
numpy>=1.24
scipy>=1.10  # pandas Spearman 相关性（IC 计算）依赖
