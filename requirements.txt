# stock-analyst dependencies
# Python 3.10+ required

# Core data
akshare>=1.18  # A 股新闻源
pandas>=2.0
requests>=2.31  # Finnhub HTTP 客户端；信息储备层 EDGAR 也用它
PySocks>=1.7  # 信息储备层访问 SEC EDGAR 走 SOCKS5 系统代理时需要（requests[socks]）
tickflow[all]  # Tickflow 行情 / K 线 / 标的信息主源

# Markets
yfinance>=0.2  # 美股盘前 / 盘后数据；需要代理才能稳定访问 Yahoo Finance

# Optional LLM provider（未配置时核心分析与 PDF 仍可运行）
anthropic>=0.40
httpx>=0.27

# PDF 渲染
markdown>=3.5
matplotlib>=3.7  # 多因子打分卡的雷达图

# 因子计算
numpy>=1.24
scipy>=1.10  # pandas Spearman 相关性（IC 计算）依赖
